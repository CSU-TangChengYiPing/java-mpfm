enum key {
  theme = "theme",
  backgroundImage = "background-image",
  sideBarOpen = "side-bar-open",
  locale = "locale",
  profileAlias = "profile-alias",
  profileAvatarVersion = "profile-avatar-version",
  compactMode = "compact-mode",
  debugTab = "debug-tab",
  fileEditModalGeometry = "file-edit-modal-geometry",
}

export default key;
