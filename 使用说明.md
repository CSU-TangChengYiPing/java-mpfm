# 使用说明

本文说明如何使用 Docker 快速部署本项目。快速部署只要求修改最少配置；更多可选配置见文末附录

注意：docker部署默认不考虑HTTPS证书的配置。
如果需要docker配置HTTPS证书，请手动解决。

## Docker 快速部署

### 1. 复制环境变量文件

```bash
cp .env.example .env
```

### 2. 最小修改 `.env`

由于.env默认是非docker部署的，所以部分配置需要更改。
Docker 部署时，容器内访问数据库和 Redis 不能使用 `localhost`，需要改成 Compose 服务名。

只需要修改下面两项：

```env
MPFM_DB_URL=jdbc:postgresql://postgres:5432/mpfm
MPFM_REDIS_HOST=redis
```

其他配置先保持默认即可。

### 3. 一键启动

```bash
docker compose --env-file .env -f docker-compose-deploy.yml up -d --build
```

### 4. 访问系统

默认访问地址：

```text
http://localhost
```

默认管理员账号来自 `.env`：

```env
MPFM_ROOT_USERNAME=root
MPFM_ROOT_PASSWORD=Root@123456
```

## 常用命令

查看容器状态：

```bash
docker compose --env-file .env -f docker-compose-deploy.yml ps
```

查看日志：

```bash
docker compose --env-file .env -f docker-compose-deploy.yml logs -f
```

停止服务：

```bash
docker compose --env-file .env -f docker-compose-deploy.yml down
```

重新构建并启动：

```bash
docker compose --env-file .env -f docker-compose-deploy.yml up -d --build
```

## 按需修改

### 修改访问端口

如果服务器的 80 端口被占用，修改 `docker-compose-deploy.yml` 中前端端口映射：

```yml
ports:
  - "11111:80"
```

修改后访问：

```text
http://localhost:11111
```

服务器部署时将 `localhost` 换成服务器 IP 或域名。

### 修改管理员账号

在 `.env` 中修改：

```env
MPFM_ROOT_USERNAME=你的管理员账号
MPFM_ROOT_PASSWORD=你的管理员密码
```

首次启动时会按该配置初始化管理员账号。

### 修改密钥

正式部署建议修改下面两个密钥：

```env
MPFM_JWT_SIGNING_KEY=请替换为至少32位的随机字符串
MPFM_AVATAR_SIGNING_KEY=请替换为至少32位的随机字符串
```

## 附录：Docker 部署配置说明

### 为什么最少只改两项

`.env.example` 默认使用本机开发地址：

```env
MPFM_DB_URL=jdbc:postgresql://localhost:5432/mpfm
MPFM_REDIS_HOST=localhost
```

但在 Docker Compose 中，后端容器里的 `localhost` 指的是后端容器自己，不是 PostgreSQL 或 Redis 容器。因此 Docker 部署时需要改为：

```env
MPFM_DB_URL=jdbc:postgresql://postgres:5432/mpfm
MPFM_REDIS_HOST=redis
```

数据库名、用户名、密码、Redis 端口等默认值已经和 `docker-compose-deploy.yml` 匹配，快速部署时不用改。

### 数据库配置

默认数据库配置：

```env
MPFM_DB_NAME=mpfm
MPFM_DB_USERNAME=mpfm
MPFM_DB_PASSWORD=mpfm_dev_password
MPFM_DB_URL=jdbc:postgresql://postgres:5432/mpfm
```

对应 Compose 服务名为 `postgres`。

### Redis 配置

默认 Redis 配置：

```env
MPFM_REDIS_HOST=redis
MPFM_REDIS_PORT=6379
MPFM_REDIS_PASSWORD=
```

对应 Compose 服务名为 `redis`。

### 文件存储路径

部署 Compose 会将后端数据卷挂载到容器内 `/data`：

```yml
volumes:
  - mpfm_deploy_backend_data:/data
```

如需让本地文件和头像数据明确落在该数据卷下，可以在 `.env` 中设置：

```env
MPFM_LOCAL_BASE_PATH=/data/local
MPFM_AVATAR_BASE_PATH=/data/avatars
```

快速体验时不改也可以启动；正式部署建议改为上面的容器内路径。

### 前端后端地址

前端构建参数：

```env
VITE_BACKEND_ORIGIN=http://localhost
```

如果部署到服务器并通过浏览器访问服务器地址，可按实际情况改为：

```env
VITE_BACKEND_ORIGIN=http://服务器IP
```

如果使用了自定义端口，例如 `11111:80`，则可写成：

```env
VITE_BACKEND_ORIGIN=http://服务器IP:11111
```

### nginx配置
手动去改 `./new_frontend/nginx.deploy.conf`

### 清空数据重新部署

如果需要删除数据库、Redis 和后端文件数据卷，执行：

```bash
docker compose --env-file .env -f docker-compose-deploy.yml down -v
```

注意：该命令会删除已保存的数据。
